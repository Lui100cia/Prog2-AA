// Check list of ssh created
ls ~/.ssh/

// Test connection to github
ssh -T git@github.com

// Generate a new ssh
// step 1
ssh-keygen -t ed25519 -C "your_email@example.com"
// step 2
rename it "/home/lui100cia/.ssh/id_ed25519" to "/home/lui100cia/.ssh/id_your_name"
// step 3
cat ~/.ssh/id_your_name.pub
// step 4
copy and paste the public key to github ssh settings

// Check if ssh-agent is on and activate it otherwise
eval "$(ssh-agent -s)"

// list all added ssh
ssh-add -l

// add your ssh to ssh agent (private)
ssh-add ~/.ssh/id_your_name





